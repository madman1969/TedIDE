#ifndef DELAY_H
#define DELAY_H

/* A short busy-wait pause, e.g. between palette swatches. */
void delay_short(void);

/* A longer busy-wait pause, e.g. for one note of the sound demo. */
void delay_long(void);

#endif
